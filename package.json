{
  "name": "teaching-platform-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "generate:models": "python3 scripts/generate_shared_models.py",
    "dev:web": "/home/trung/.nvm/versions/node/v22.16.0/bin/pnpm --filter web dev",
    "dev:api": "cd apps/api2 && go run ."
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
