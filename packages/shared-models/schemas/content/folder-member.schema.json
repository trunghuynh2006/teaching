{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FolderMember",
  "description": "Represents a user's membership in a folder, enabling shared access across multiple users.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "folder_id": {
      "type": "string",
      "description": "The folder this membership record belongs to."
    },
    "user_id": {
      "type": "string",
      "description": "The user who has access to the folder."
    },
    "role": {
      "type": "string",
      "enum": ["viewer", "editor"],
      "description": "Access level: viewer can read, editor can also modify folder contents."
    },
    "added_by": {
      "type": ["string", "null"],
      "default": null,
      "description": "User identifier that granted this membership."
    },
    "added_time": {
      "type": ["string", "null"],
      "format": "date-time",
      "default": null,
      "description": "Timestamp when membership was granted (ISO 8601)."
    }
  },
  "required": ["folder_id", "user_id", "role"]
}
