{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ConceptAlias",
  "description": "An alternative name for a concept, used for deduplication and search.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique alias identifier."
    },
    "concept_id": {
      "type": "string",
      "description": "The concept this alias refers to."
    },
    "alias": {
      "type": "string",
      "description": "Alternative name or spelling for the concept."
    },
    "created_by": {
      "type": ["string", "null"],
      "default": null,
      "description": "User identifier that created this record."
    },
    "created_time": {
      "type": ["string", "null"],
      "format": "date-time",
      "default": null,
      "description": "Record creation timestamp (ISO 8601)."
    }
  },
  "required": ["id", "concept_id", "alias"]
}
