{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SourceTopic",
  "description": "Many-to-many mapping between a Source (Knowledge) and a Topic.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "source_id": {
      "type": "string",
      "description": "The Knowledge entry (source) that covers this topic."
    },
    "topic_id": {
      "type": "string",
      "description": "The topic covered by this source."
    },
    "created_by": {
      "type": ["string", "null"],
      "default": null,
      "description": "User identifier that created this record."
    },
    "created_time": {
      "type": ["string", "null"],
      "format": "date-time",
      "default": null,
      "description": "Record creation timestamp (ISO 8601)."
    }
  },
  "required": ["source_id", "topic_id"]
}
