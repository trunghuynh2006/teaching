{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SourceConcept",
  "description": "Many-to-many mapping between a Source (Knowledge) and a Concept.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "source_id": {
      "type": "string",
      "description": "The Knowledge entry (source) that covers this concept."
    },
    "concept_id": {
      "type": "string",
      "description": "The concept covered by this source."
    },
    "created_by": {
      "type": ["string", "null"],
      "default": null,
      "description": "User identifier that created this record."
    },
    "created_time": {
      "type": ["string", "null"],
      "format": "date-time",
      "default": null,
      "description": "Record creation timestamp (ISO 8601)."
    }
  },
  "required": ["source_id", "concept_id"]
}
