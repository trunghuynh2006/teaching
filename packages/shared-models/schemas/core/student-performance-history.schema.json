{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StudentPerformanceHistory",
  "description": "Immutable time-based snapshots of learner performance per skill.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique history entry identifier."
    },
    "student_performance_id": {
      "type": "string",
      "description": "Referenced student performance rollup identifier."
    },
    "learner_id": {
      "type": "string",
      "format": "uuid",
      "description": "Referenced learner (student profile) identifier."
    },
    "skill_id": {
      "type": "string",
      "description": "Referenced skill identifier."
    },
    "score": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "description": "Observed normalized performance score in range 0-100."
    },
    "level_framework": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Optional proficiency framework identifier at this snapshot (for example cefr)."
    },
    "level_value": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Optional level value within the selected framework at this snapshot (for example B2)."
    },
    "recorded_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this performance snapshot was recorded (ISO 8601)."
    },
    "source": {
      "type": "string",
      "enum": [
        "assessment",
        "assignment",
        "manual",
        "system"
      ],
      "default": "system",
      "description": "Source of this measurement."
    },
    "notes": {
      "type": [
        "string",
        "null"
      ],
      "default": null,
      "description": "Optional notes about the measurement."
    }
  },
  "required": [
    "id",
    "student_performance_id",
    "learner_id",
    "skill_id",
    "score",
    "recorded_at"
  ]
}
